# Как загрузить server-example на GitHub

## ⚠️ Проблема: "Too many files"

GitHub веб-интерфейс не позволяет загружать больше 100 файлов за раз. В папке `node_modules` тысячи файлов!

## ✅ Решение: Используйте Git (рекомендуется)

### Вариант 1: Через Git командную строку (лучший способ)

1. **Убедитесь, что node_modules удален:**
   ```bash
   # В папке server-example не должно быть node_modules
   ```

2. **Откройте PowerShell в папке server-example:**
   ```bash
   cd server-example
   ```

3. **Инициализируйте Git:**
   ```bash
   git init
   ```

4. **Добавьте файлы:**
   ```bash
   git add .
   ```

5. **Сделайте первый коммит:**
   ```bash
   git commit -m "Initial commit: LiveTalk server"
   ```

6. **Создайте репозиторий на GitHub:**
   - Зайдите на https://github.com
   - Нажмите "New" → создайте репозиторий
   - НЕ добавляйте README, .gitignore и лицензию (они уже есть)
   - Можете сделать приватным

7. **Подключите удаленный репозиторий:**
   ```bash
   git remote add origin https://github.com/YOUR_USERNAME/livetalk-server.git
   ```
   (Замените YOUR_USERNAME на ваш GitHub username)

8. **Загрузите код:**
   ```bash
   git branch -M main
   git push -u origin main
   ```

9. **Если GitHub попросит авторизацию:**
   - Используйте Personal Access Token вместо пароля
   - Создайте токен: GitHub → Settings → Developer settings → Personal access tokens → Generate new token
   - Дайте права: `repo` (полный доступ к репозиториям)

### Вариант 2: Через GitHub Desktop (проще для новичков)

1. **Скачайте GitHub Desktop:**
   - https://desktop.github.com
   - Установите и войдите в аккаунт

2. **File → Add Local Repository**
   - Выберите папку `server-example`
   - GitHub Desktop автоматически увидит, что это Git репозиторий

3. **Внизу введите:**
   - Name: `livetalk-server`
   - Description: "Server for LiveTalk messenger"
   - ✅ Поставьте галочку "Keep this code private" (если хотите приватный)

4. **Нажмите "Publish repository"**

5. **Готово!** Код загружен на GitHub

### Вариант 3: Через веб-интерфейс (только нужные файлы)

Если хотите использовать веб-загрузку, загружайте файлы по одному:

1. **Создайте репозиторий на GitHub** (пустой)

2. **Загрузите файлы по одному:**
   - Нажмите "uploading an existing file"
   - Загрузите:
     - `server.js`
     - `package.json`
     - `README.md`
     - `.gitignore`
     - `Dockerfile` (опционально)
     - `fly.toml` (опционально)

3. **НЕ загружайте:**
   - ❌ `node_modules/` (будет создан при `npm install`)
   - ❌ `package-lock.json` (можно, но не обязательно)

## 📝 Важно!

**node_modules НЕ нужно загружать на GitHub!**

- Он уже указан в `.gitignore`
- При деплое на Cyclic.sh выполнится `npm install` и зависимости установятся автоматически
- Это стандартная практика для Node.js проектов

## ✅ Проверка

После загрузки в репозитории должны быть только эти файлы:
- ✅ server.js
- ✅ package.json
- ✅ README.md
- ✅ .gitignore
- ✅ Dockerfile (опционально)
- ✅ fly.toml (опционально)

**НЕ должно быть:**
- ❌ node_modules/
- ❌ package-lock.json (можно, но не обязательно)

## 🚀 После загрузки

Следуйте инструкции в `CYCLIC_DEPLOY.md` для деплоя на Cyclic.sh!

